'use client';

import { ActividadesPlaceholder } from '../../../../components/actividades/ActividadesPlaceholder';

/* CAL-001 — Áreas placeholder. The configurable area-catalog config screen lands in
   CAL-002 (list + form + palette + activate/deactivate + delete). */
export default function ActividadesAreasPage() {
  return (
    <ActividadesPlaceholder
      title="Áreas"
      description="Catálogo configurable de áreas para organizar las actividades."
    />
  );
}
